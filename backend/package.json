{
  "name": "react-hosting-be",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "brew services restart mongodb-community && nodemon src/server.js",
    "start": "node src/server.js",
    "build": "cd ../front-end && npm install && npm run build && cp -r dist ../backend/dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "firebase-admin": "^13.2.0",
    "mongodb": "^6.15.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
